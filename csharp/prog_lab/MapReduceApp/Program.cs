using System;

public class Program {
    /*
    public static void Main(){
        var m = new MapReduce<int>{};


        DataReader dr = new DataReader("test.10000");

        //Console.WriteLine(dr[99999]);

        foreach (double data in dr){
            Console.WriteLine(data);
        }

        //m.Add(2);
        //m.Add(3);
        //m.Add(7);

        //m.Map(x => x*10);

        //for (int i = 0; i < m.Count; i++){
            //Console.WriteLine(m[i]);
        //}

        //var accum = m.Reduce((left, right) => (left+right)/5);

        //var rtask = m.ReduceAsync((left, right) => (left+right)/5);

        //rtask.Wait();

        //Console.WriteLine(accum);
        //Console.WriteLine($"accum value: {rtask.Result}");
    }
    */
}